[project]
name = "goated-ai-skills"
version = "0.1.0"
description = "Maintenance tooling for GOATED AI Skills."
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.25.0",
    "pyyaml>=6.0.2",
]

[tool.uv]
package = false
