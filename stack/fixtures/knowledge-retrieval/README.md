# Knowledge Retrieval Fixtures

These fixtures exercise the observable `knowledge-retrieval` contract using
ordinary project files. They cover progressive read-only search, claim-scoped
ranking, shared-evidence output, conflict and staleness reporting, optional
capability fallback, and the boundary with writer-focused `learning-capture`.

The fixtures describe portable outcomes rather than a search command, note
application, database, connector, or semantic index.
