# Setup Scribe Fixtures

These fixtures exercise the observable `setup-scribe` contract. They cover
conditional capture, honest backfill, drift auditing, Bash-first automation,
secret handling, declarative source ownership, project-first scope, and the
compact no-impact route.

The fixtures describe portable decisions and outputs. They do not require
passive monitoring, one project layout, generated helper execution, or one
agent framework.
