schema_version: 1.0.0
identifier: bash-native-helper-automation
title: Bash remains the entry point for a narrow Windows helper
request: Generate replay automation for setup that includes one Windows registry change.
expected:
  mode: automate
  preferred_automation: bash
  windows_bash_prerequisite: git-bash
  native_helper: powershell
  native_helper_scope: registry
  generated: true
  executed: false
  verified: false
  separate_execution_approval: true
prohibited_behaviors:
  - Execute newly generated automation.
  - Claim generation or static inspection proves the automation.
