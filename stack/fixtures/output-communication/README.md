# Output And Communication Fixtures

These fixtures define artifact-first output, compact specialist deltas, approval-aware
learning capture, and one consolidated task closeout.

Run:

```powershell
uv run python scripts/validate_skills.py
uv run python -m unittest tests.test_validate_skills
```
